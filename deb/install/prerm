rm -r /etc/sage-utils/dhcp-pxe/

rm /etc/exports
mv /etc/exports.old /etc/exports

rm /etc/dnsmasq.conf
mv /etc/dnsmasq.conf.old /etc/dnsmasq.conf

exportfs -ra
systemctl restart dnsmasq
systemctl restart rpcbind
systemctl restart nfs-kernel-server
